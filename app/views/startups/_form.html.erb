<div class="row">

  <div class="large-9 small-centered columns text-center">
    <p>To enable Angellist follows, you must have an Angellist ID. You can attempt to find one by entering the company name and clicking the 'Find Angellist ID' button.</p>
    <p>The CEO image needs to be added to the assets/images folder in the 'companyname.jpg' format.</p>
  </div>

  <div class="large-5 small-centered columns text-center">

    <!--<button id="pull-from-angellist" class="button alert radius">Pull Info From Angellist</button>-->
    <button id="pull-angellist-id" class="button alert radius">Find Angellist ID</button>
  
    <br /><br />

    <%= form_for @startup do |f| %>

    <div class="row">
      <div class="large-6 small-centered columns">
        <%= f.label "Angellist Id:" %>
        <%= f.text_field :angellist_id %>
      </div>
    </div>

      <div class="row">

      <%= f.label "Company Name:" %>
      <%= f.text_field :name %>

      <%= f.label "Company Bio:" %>
      <%= f.text_field :bio %>

      <%= f.label "Pitched By:" %>
      <%= f.text_field :ceo_name %>

      <%= f.label "Contact Email Address:" %>
      <%= f.text_field :email %>

      <%= f.label "Presentation Order:" %>
      <%= f.text_field :presentation_order %>
      
      <%= f.submit :class => 'button radius alert' %>

    <% end %>

  </div>
</div>